# Link checker for AsciiDoc in the Antora hub.
exclude_path = [
  ".git",
  "node_modules",
  "build",
  ".vale",
]

# Local / placeholder URLs in examples.
exclude = [
  '^http://localhost',
  '^https://localhost',
  '^https://unpkg.com/',
  '^https://cdn.jsdelivr.net/',
  '^https://proxy.scalar.com/',
  # Present locally; remote 404 until CHANGELOG merges to main.
  '^https://github.com/sophias-hub/notification-hub-service/blob/.*/CHANGELOG',
]
