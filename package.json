{
  "name": "docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:build": "antora antora-playbook.yml",
    "validate": "bash scripts/validate.sh"
  },
  "scriptsComments": {
    "docs:build": "Build the Antora hub into build/site/",
    "validate": "Lint AsciiDoc prose (Vale) and check links (Lychee)"
  },
  "devDependencies": {
    "@antora/cli": "^3.1.10",
    "@antora/site-generator-default": "^3.1.10"
  }
}

