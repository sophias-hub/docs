= Reference: SDK

The live SDK reference is a published TypeDoc site generated from the TypeScript client in `notification-hub-service`.

== Published reference

* SDK reference (TypeDoc): https://sophias-hub.github.io/docs-sdk/

== What it covers

* `NotificationClient` and related types
* Usage notes from `src/sdk/README.md`

== Local preview

From `notification-hub-service`:

[source,bash]
----
npm run docs:sdk
open docs/sdk/index.html
----

== Source of truth

* SDK source: `notification-hub-service/src/sdk/`
* Published on every successful `main` CI run into the `docs-sdk` GitHub Pages repo
